# Permit Jeff deployments to restart the existing service without a password.
# Keep the service unit installed separately with deploy/install.sh.
tray ALL=(root) NOPASSWD: /usr/bin/systemctl restart jeff.service
